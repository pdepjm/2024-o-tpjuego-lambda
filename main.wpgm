import wollok.game.*
import isaac.*
import nivel.*
import obstaculos.*


program  isaacGame {
	
    const musica = game.sound("fondoMusica.mp3")
	musica.shouldLoop(true)
    game.schedule(500, { musica.play()} )

	nivel.inicio()
	game.start()
}
